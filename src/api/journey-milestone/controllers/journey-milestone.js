'use strict';

/**
 * journey-milestone controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::journey-milestone.journey-milestone');
